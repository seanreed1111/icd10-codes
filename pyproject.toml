[project]
name = "anthuria-project"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "lxml>=6.0.2",
    "pandas>=3.0.1",
    "requests>=2.32.5",
]
